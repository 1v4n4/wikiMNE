import { Typography } from '@mui/material';
import React from 'react';

interface Props {}

const Kontakt: React.FC<Props> = (props) => {

  return (
    <Typography variant="h3">Kontakt</Typography>
  )
};

export default Kontakt;