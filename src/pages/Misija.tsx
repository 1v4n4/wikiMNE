import { Typography } from '@mui/material';
import React from 'react';

interface Props {}

const About: React.FC<Props> = (props) => {

  return (
    <Typography variant="h3">Misija</Typography>
  )
};

export default About;